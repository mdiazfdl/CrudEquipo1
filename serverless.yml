service: CrudEquipo1
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x

plugins:
  - serverless-offline
  - serverless-dynamodb

custom:
  serverless-dynamodb:
    stages:
      - dev
    start:
      port: 8000
      inMemory: true
      migrate: true

resources:
  Resources:
    userTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: userTable
        AttributeDefinitions:
          - AttributeName: id
            AttributeType: S
        KeySchema:
          - AttributeName: id
            KeyType: HASH
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1

functions:
  api:
    handler: index.handler
    events:
      - httpApi:
          path: /
          method: get
  getAllUsers:
    handler: src/Users/listUsers.getAllUsers
    events:
      - httpApi:
          path: /listUsers
          method: get
  createUser:
    handler: src/Users/createUser.createUser
    events:
      - httpApi:
          path: /createUser
          method: post